# PHASE2_SPEC.md — Calibration & Validity (binding)

Completion promise: `PAPER_VALIDITY_COMPLETE`. Branch: `phase2-calibration` (off autonomous-build). All Phase-1 HUMAN gates stay. Platforms stay mocked; no cloud; no publish/DOI/visibility change; no real URL/DOI in manuscript; every figure number traces to results/; never self-approve HUMAN tasks.

Context: Phase 1 built a deterministic rig, but the executed numbers do not substantiate the paper's structural claims and in places contradict them (e.g., near-identical latencies across patterns/scenarios, P6 HITL not human-dominated, fault matrix all-contained, cost artifacts). Phase 2 makes results CREDIBLE and CONSISTENT WITH THE PAPER, proven by an automated agreement gate.

## PRIMARY DELIVERABLE — tests/test_paper_agreement.py
Encodes the paper's Table 3 + Table 4 directional claims; asserts executed results (from a study run or results/ fixtures regenerated in-test at adequate n) satisfy them. Must assert AT LEAST:
1. P2 Sequential Pipeline p99 in S2 (multi-step) > P2 p99 in S1 (additive over stages).
2. P6 HITL end-to-end latency is the HIGHEST of all patterns / human-step-dominated, not the lowest (configurable human-decision delay; HITL latency-bound per Table 3).
3. P3 Event-Driven Choreography degrades LEAST under S3 bursty load relative to P1/P2 (burst absorption — compare p99 inflation S3 vs S1 baseline per pattern).
4. Fault campaign shows PROPAGATION for P2 (stage fault blocks downstream) and SPOF behaviour for P1 (supervisor outage) and P4 (blackboard/memory-store outage); CONTAINMENT (isolated/absorbed) for P3, P5 (bulkhead/timeout), P7 (platform outage contained to one cluster).
5. Fault campaign exercises human_queue faults for P6 and shows correctness isolation.
6. Per-condition results DIFFER across S1/S2/S3 (no identical-to-13-decimals values across scenarios for the same pattern/platform).

## Fixes (tasks; implement, test, commit per task; new prd tasks 101+ appended to prd.json)
- **101 (AUTO) Structural latency derivation.** Latency must EMERGE from pattern structure: P1 = plan + max(collaborator latencies) with p99 worsening with fan-out (tail-at-scale; cite Dean & Barroso 2013 in code docstring + SAP); P2 = additive over stages (S2 multi-step >> S1); P3 = queue/burst-absorbed (decoupled consumption); P4 = contention-bound (latency rises with concurrent writers); P5 = one gateway hop over base; P6 = dominated by configurable human-decision delay (configs: human_decision_delay mean/sigma); P7 = local + cross-platform RTT term. Calibrate magnitudes to realistic LLM-agent ranges: single model step p50 in the high-hundreds of ms to a few s; full multi-step seconds; HITL tens of seconds via human delay. NO 60–190 s latencies anywhere in baseline. NO unexplained cross-platform gaps (platform deltas must come from documented config, e.g., gateway hop or runtime overhead, small and explained).
- **102 (AUTO) Load generator below saturation.** Open-loop retained; measure single-replica saturation per condition first (rig/saturation), then set arrival rate to a configured fraction (e.g., 0.7) of measured saturation; report the saturation check in results/manifest.json. Raise S1 rate so it genuinely stresses coordination/cost. p99 must not be an unbounded-queue artifact (assert utilization < 1 in agreement suite or loadgen test).
- **103 (AUTO) Independent random streams per condition (Option A).** Remove common-random-numbers coupling: each (pattern, platform, scenario, replicate) condition gets an independent child stream (seed derived from master seed + full condition name). MWU independence assumption then holds and matches the SAP. Re-validate bootstrap coverage and MWU on synthetic shifts. Update SAP accordingly [SAP edit stays HUMAN-flagged].
- **104 (AUTO) Fault campaign realism.** Injected faults must actually degrade the affected pattern (error_rate and/or latency rises under fault vs baseline; assert in tests). Implement cross-request fault state where the paper implies it (component outage windows affect all requests traversing during the window; P1 supervisor outage = SPOF; P4 blackboard store outage = SPOF; P2 stage fault blocks downstream stages of that request and queued requests during outage; P3 buffered events redelivered after outage = contained; P5 bulkhead confines tool fault; P7 remote-cluster outage degrades only bridged work). Exercise each pattern against the faults relevant to its own components (cut "not exercised" cells to the structurally irrelevant ones only). Matrix must show a MIX (propagated / isolated / absorbed) matching Table 3's consequences.
- **105 (HUMAN) Cost fix.** Agentforce Agent Script actions still consume conversation/credit cost (no 0-billed paths). ONE consistent unit (USD per request under dated assumptions). Realistic relative costs (multi-step > single-step; HITL adds human cost only if the paper says so — else note). All prices dated assumptions [HUMAN: confirm unit prices]. Regenerate ledger.
- **106 (HUMAN) Table 4 derived from data.** Grades computed from calibrated numbers + MWU/Holm comparisons (NOT hand rules): document the grade function (e.g., A if in best Holm-significant group on the scenario's primary endpoint, C if worst / significantly dominated, B otherwise; oversight column from HITL capability not latency). Agreement suite asserts directional match to the paper's fit-for-purpose matrix: S3 -> P3 strong / P2 weak; S2 -> P1, P2 strong; P6 -> latency-weak but oversight-strong.
- **107 (HUMAN) AI-use disclosure rewrite.** docs/manuscript/ai_use_disclosure.md: accurately states an AI pipeline (Claude-based) generated the reference implementation, the evaluation rig, the executed results and figures, and drafted manuscript sections; the author reviewed, verified, and owns all content. IEEE AI-disclosure-policy-conformant wording.
- **108 (HUMAN) Mock-fidelity prominence.** Results section + threats-to-validity: prominent caveat that results are RELATIVE structural comparisons under identical controlled mock conditions, not production SLAs. Update results_section.md to the recalibrated numbers (all traces refreshed).
- **109 (HUMAN) Re-flag for review against recalibrated artifact:** REJECTED_CANDIDATES (052), governance mappings (045–047) — re-read each against the new artifact, adjust any rig-evidence pointers, append re-flag lines to progress.txt.
- **110 (AUTO) Regenerate everything.** bash run.sh regenerates all tables/figures from the calibrated rig; update RESULTS_TO_PAPER_MAP if outputs changed; demo.sh still green.
- **111 (AUTO) Final gate.** tests/test_paper_agreement.py green AND pytest --cov green AND run.sh green AND only HUMAN items remain -> append PAPER_VALIDITY_COMPLETE to progress.txt.

## Done definition
tests/test_paper_agreement.py passes AND pytest --cov passes AND run.sh regenerates all outputs AND recalibrated Table 3/Table 4 reproduce the paper directionally AND only HUMAN-gated items remain. Record before/after numbers for P1/P2/P4/P6 (p50/p99, per scenario) in docs/PHASE2_BEFORE_AFTER.md as part of task 110.
