# capsule/code — pointer

The capsule's code is the repository root:

- Package: `../../src/agentorch/`
- Governance demo: `../../governance/hitl_example.py`
- Tests: `../../tests/`
- Entry point (full reproduction): `../../run.sh`
- Entry point (quick demo): `../../demo.sh`

See the top-level `../../README.md` (Requirements / Installation /
Usage / Expected Output / License).
